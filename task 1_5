while True:
    a = input('Enter the name : ')

    if len(a)>7:
        break

    def Convert(st):
        list1 = []
        list1[:0] = a
        return list1
    list2=Convert(a)
    if list2[0]==list2[-1]:
        print('good')
